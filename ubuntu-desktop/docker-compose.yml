# Docker Compose 파일 포맷 버전 지정
version: "2.2"
services:
  ubuntu:
    container_name: ubuntu-desktop
    build:
      context: ./ubuntu2404
      dockerfile: Dockerfile
    image: oseongryu/ubuntu-desktop:20250820.01
    ports:
      - "13387:3389"
    restart: always
    stdin_open: true # docker run -i
    tty: true # docker run -t
    privileged: true
